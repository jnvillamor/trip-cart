/**
 * App + data versions surfaced in About and used by data import/export.
 * Bump SCHEMA_VERSION whenever the import/export JSON shape changes.
 */
export const APP_VERSION = '1.0.0';
export const SCHEMA_VERSION = 1;
