export enum TRIP_STATUS_ENUM {
  PLANNED = 'planned',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  CANCELED = 'canceled',
}

export enum THEME_MODE_ENUM {
  LIGHT = 'light',
  DARK = 'dark',
  SYSTEM = 'system',
}